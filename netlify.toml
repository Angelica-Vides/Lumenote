# Manual CLI deploy — run: npm run deploy:netlify
# Do NOT connect GitHub auto-deploy in the Netlify dashboard (saves free-tier credits).

[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
